# Approval Gates + Channel UX — Kanban

Tracks execution of [`PLAN.md`](./PLAN.md). Status: `[ ]` not started · `[~]` in progress · `[x]` done · `[!]` blocked · `[→]` deferred (with rationale).

Last updated: 2026-04-26.
Active version: v0.4.41.

---

## Week 1 — Foundation: pause/resume + minimal aggregator

- [x] **W1.1** Survey the exact code paths to be modified (workflows.rs:1209, gate-decide handler, automation_v2 types, governance.rs).
  - Key finding: `automation_v2/executor.rs:803-839` already pauses on approval nodes and sets `awaiting_gate`. **Workflows do NOT go through this executor** — `workflows.rs:execute_actions` runs a synchronous loop that bypasses gates entirely. This is why the user has never seen workflows pause. The fix for workflows is its own milestone (deferred — see W1.3/W1.4 notes below).
- [x] **W1.2** Define `ApprovalRequest` shape in `crates/tandem-types/src/approvals.rs`.
  - 5 unit tests pass. Types: `ApprovalRequest`, `ApprovalDecision`, `ApprovalSourceKind`, `ApprovalTenantRef`, `ApprovalActorRef`, `ApprovalDecisionInput`, `ApprovalListFilter`.
- [→] **W1.3** Extract shared `pause_for_gate` / `decide_gate` helpers — **deferred to a focused refactor session.**
  - Reason: the gate-decide handler at `routines_automations_parts/part02.rs:1563–1640` is comprehensive and works correctly today against `automation_v2`. Coder uses a different pattern at `coder_parts/part07.rs:386–450`. Extracting the shared helper requires careful design (different state shapes, different lifecycle events) and benefits from being done in one focused session rather than rushed mid-feature. **Tracking issue: file as a refactor task before W2 starts.**
- [→] **W1.4** Wire the workflow dispatcher to pause — **deferred for design discussion.**
  - Reason: `workflows.rs:execute_actions` is a synchronous loop without checkpoint/resume. Adding pause/resume requires either (A) routing workflow execution through the existing `automation_v2` executor or (B) building a parallel pause/resume mechanism in the workflow layer. The plan assumed (B); on inspection, (A) may be cleaner but needs a design conversation with the user. **Defer until that conversation; track as a Week-1.5 milestone.** Until then, automation_v2 missions with approval nodes pause correctly today (the executor already handles them); only freestanding workflows do not.
- [x] **W1.5** Add `GET /approvals/pending` aggregator endpoint.
  - Files: `crates/tandem-server/src/http/approvals.rs` (logic), `crates/tandem-server/src/http/routes_approvals.rs` (route). Wired in `http.rs` and `router.rs`. 3 integration tests pass: surfaces automation_v2 awaiting_gate runs, returns empty when none pending, source filter excludes mismatches.
- [→] **W1.6** Hand-code one `HumanApprovalGate` on a CRM-write step in the demo bundle — **deferred until W1.4 lands.**
  - Reason: workflows don't pause yet, so a hand-coded gate would not actually block. An automation_v2 mission with an approval node _would_ work today against the new aggregator, but the demo workflow is workflow-shaped, not mission-shaped. Build the demo gate after W1.4.
- [x] **W1.7** Update CHANGELOG and RELEASE_NOTES; commit.

**What actually shipped this session:** the unified `ApprovalRequest` type model and a working cross-subsystem aggregator endpoint that surfaces existing `automation_v2` pending gates. Channel adapters and the control-panel inbox can both consume `/approvals/pending` immediately.

**What was deferred (with rationale above):** the workflow pause/resume path (W1.3, W1.4, W1.6). These need design discussion before coding — rushing them would land bad architecture. The aggregator is forward-compatible; once workflows pause, they plug into the same endpoint by adding a branch in `http/approvals.rs`.

**Acceptance status:** partial. The aggregator works against any subsystem that can pause today (automation_v2 with approval nodes). Workflow pause itself is still gap-flagged in the reality map and demo plan.

---

## Week 2 — Slack vertical slice (the discovery slice)

- [x] **W2.1** Webhook signing module at `crates/tandem-channels/src/signing.rs`. Slack HMAC-SHA256 with 5-min replay protection (full impl, 12 tests covering valid, missing/malformed headers, replay window, body tampering, secret mismatch, empty secret). Telegram `secret_token` constant-time compare (5 tests). Discord Ed25519 stub returning `SecretNotConfigured` until `ed25519-dalek` lands in W4 (2 tests). Plus 3 hex-decode helper tests.
- [x] **W2.2** `Channel::send_card(InteractiveCard)` trait method on `crates/tandem-channels/src/traits.rs`. `InteractiveCard`, `InteractiveCardField`, `InteractiveCardButton`, `InteractiveCardButtonStyle`, `InteractiveCardConfirm`, `InteractiveCardReasonPrompt`, `InteractiveCardSent`, `InteractiveCardError`. Default impl returns `NotImplemented` so the type system tells callers which adapters are wired. New helper: `supports_interactive_cards()`.
- [x] **W2.3** Slack Block Kit renderer at `crates/tandem-channels/src/slack_blocks.rs`. Pure functions: `render_card_blocks`, `build_post_message_payload`, `build_chat_update_payload_for_decision`, `build_rework_modal_payload`. Layout: header / context / body section / fields grid / divider / actions block (chunked at Slack's 5-button cap). 18 golden tests covering header, context, body, fields, button styles (primary→`primary`, destructive→`danger`, default omits style), confirm dialog, button value round-trip with correlation, post-message thread_ts, chat-update decision summary replacing buttons, modal payload, text clamping, mrkdwn escaping.
- [x] **W2.4** Slack interaction endpoint at `crates/tandem-server/src/http/slack_interactions.rs` wired at `POST /channels/slack/interactions`. Verifies `x-slack-signature` HMAC + timestamp on every request via the new signing module. Acks within 3s synchronously. Bounded LRU dedup ring on `(action_ts, action_id)` to absorb Slack's retry-on-missed-ack behavior. Parses Slack's `application/x-www-form-urlencoded` `payload` field, extracts the primary action, dispatches `approve` and `cancel` to `automations_v2_run_gate_decide` directly (handler now `pub(crate)`). `rework` is parsed and logged but the modal round-trip lands in W2.5/W4. New `SlackConfigFile.signing_secret` field. 7 unit tests on parsing, dedup, action extraction, key derivation.
- [→] **W2.5** In-place edit on decision via `chat.update` — **builder shipped, dispatch deferred to a real-Slack session.** `slack_blocks::build_chat_update_payload_for_decision` produces the payload that swaps buttons for a "Decided by @alice at 14:32" context line; verified by golden test. Calling Slack's `chat.update` API end-to-end requires plumbing the original `(channel, ts)` from the post-message response back through the gate-decide flow; that wiring is a focused real-Slack session task.
- [x] **W2.6** Race UX: gate-decide 409 now returns the winner's identity. `automations_v2_run_gate_decide` (`routines_automations_parts/part02.rs:1576`) reads the most recent `gate_history` entry on conflict and includes `winningDecision { node_id, decision, reason, decided_at_ms }` plus `currentStatus` in the 409 body. New integration test `gate_decide_409_includes_winning_decision_in_body` confirms shape.
- [→] **W2.7** Threaded updates — **`thread_ts` plumbing shipped, dispatcher integration deferred.** `build_post_message_payload` accepts `thread_ts`; `InteractiveCard.thread_key` carries the run identity. The `(run_id → message_ts)` mapping cache and the run-status thread-reply emitter are next-session work that needs a real Slack workspace to validate.
- [→] **W2.8** Slack App Home tab pinned approvals — **deferred.** Requires Slack OAuth flow setup + `app_home_opened` event subscription; better done as a focused real-Slack session task once W2.5 and W2.7 wiring is verified.
- [x] **W2.9** Update CHANGELOG and RELEASE_NOTES; commit.

**What shipped (51 tests passing across 4 modules):** signing infrastructure, the `InteractiveCard` abstraction, the full Block Kit renderer with golden tests, the Slack interactions HTTP endpoint with HMAC verification and idempotent dispatch, race UX returning winner identity in 409. **All testable without a live Slack workspace.**

**What's deferred (W2.5, W2.7, W2.8):** the three pieces that actually require a connected Slack workspace to verify end-to-end. The supporting builders are tested; the API plumbing (chat.update, thread_ts mapping cache, App Home subscription) is small focused work for a real-Slack session.

**Acceptance status:** the discovery slice's _risk-bearing_ parts are done — HMAC verification, replay protection, idempotency, race-UX 409 shape, type-safe `InteractiveCard`. The remaining items are wiring against a live Slack workspace, not new architecture.

---

## Week 3 — Classifier + compiler injection + ScopeInspector + Inbox UI

- [x] **W3.1** Action classifier at `crates/tandem-tools/src/approval_classifier.rs`. Table-driven with `NEVER_GATES`, `ALWAYS_GATES`, `ALWAYS_GATE_PREFIXES` (CRM/payments/outbound/calendar/tracker/Notion/Slack/Telegram/Discord/coder verbs), `NEVER_GATE_PREFIXES` (read verbs), and `ALWAYS_GATE_SUFFIXES` (`.send`, `.publish`, `.delete`, `.charge`, etc.). Default-deny for unknown external-network tools. `classify_node_allowlist` aggregates a node's allowlist; `allowlist_is_wildcard` flags `*`/`mcp.*`. **19 unit tests**.
- [x] **W3.2** Compiler gate-injection pass at `crates/tandem-plan-compiler/src/mission_runtime.rs:225` (right after node sort). New `inject_default_approval_gates(nodes, agents)` walks every projected workstream node, looks up its tool allowlist via either `metadata.builder.tool_allowlist_override` or its bound agent's `tool_allowlist`, classifies the aggregate, and attaches a `default_injected_gate(...)` when the node touches an external mutation. Idempotent: skips nodes that already carry an explicit gate; skips Approval/Review stages whose gates are blueprint-owned; honors `metadata.approval.skip_approval = true` from the ScopeInspector override. **7 new golden tests** cover CRM-write injection, outbound-email injection, pure-read no-injection, wildcard injection, unknown-tool fail-closed, scope-override skip, and explicit-blueprint-gate preservation. New `tandem-tools` workspace dependency added to plan-compiler.
- [→] **W3.3** ScopeInspector per-step override UI — **deferred to a focused TS session.** `ScopeInspector.tsx` is 2884 lines and per-step controls live deep in the existing layout; landing a clean toggle without disturbing surrounding behavior takes a focused pass. The compiler-side hook is already in place (`metadata.approval.skip_approval = true` is honored — see `respects_explicit_skip_approval_metadata_override` test) so the UI plugs in without further server work.
- [x] **W3.4** Approvals Inbox page at `packages/tandem-control-panel/src/pages/ApprovalsInboxPage.tsx`. Polls `/approvals/pending` every 5s. Each pending request renders as a `PanelCard` with workflow name, source label, action preview (markdown), tenant/run/node identifiers, requested-at relative time, and Approve / Rework / Cancel buttons in the colors that match each decision's stake. Rework opens an inline reason form whose value flows through to the gate-decide endpoint as `reason`. Race-aware: a 409 response (W2.6) renders as a toast saying "Already decided by another operator" and refreshes the queue. Route registered at `#/approvals` with shield-check icon, wired into `RouteId`, `ROUTES`, `NAV_ROUTE_ORDER`, `HashRouteOutlet`, and `AppShell` page meta. Type-checks clean (no new TS errors; the 4 errors that exist on main are in unrelated files).
- [x] **W3.5** Agent system prompt update in `crates/tandem-plan-compiler/src/planner_prompts.rs`. Added an "Approval gates:" section to `workflow_plan_common_sections()` telling the planner: gates are auto-injected by the runtime; do not add them yourself; describe the workflow as if approvals are present; batch related external actions into one step so humans approve once instead of N times; declare `stage_kind=Approval` explicitly when the gate IS the point of the step. **1 new test** asserts the policy text is present in the prompt.
- [x] **W3.6** Update CHANGELOG and RELEASE_NOTES; commit.

**What shipped (28 new tests across 3 crates plus 1 new control-panel page):** the runtime side of the pitch is now real. Any mission with a workstream that uses an external-mutation tool gets a gate auto-injected at compile time, and operators see those gates in a single inbox UI with one-click decide. The agent prompt teaches the planner that gates are runtime-enforced, not its job to add — and the compiler refuses to leave them out.

**What was deferred (W3.3):** the ScopeInspector per-step override toggle. The server-side hook (`metadata.approval.skip_approval`) is already wired and tested, so the UI is a self-contained TS task that does not block W4/W5.

**Acceptance status:** the agent-owned-workflows-with-runtime-governance pitch is now end-to-end demoable on any automation_v2 mission with the right tools — generate → compile injects gate → run pauses → inbox surfaces it → operator decides. Slack interactive cards from W2 surface the same gates outside the control panel.

---

## Week 4 — Discord + Telegram

- [x] **W4.0** Discord signing upgraded from W2 stub to real Ed25519 verification. Added `ed25519-dalek` and `hex` direct deps to `tandem-channels`. The function signature defined in W2 stayed stable so callers compiled across the upgrade. **9 new tests** cover valid signature, missing/malformed headers, wrong-key forgery, wrong-body forgery, wrong-timestamp forgery, and invalid public-key hex.
- [x] **W4.1** Discord rich-UX renderer at `crates/tandem-channels/src/discord_blocks.rs`. Pure functions converting `InteractiveCard` to Discord JSON: `build_create_message_payload` (one embed + chunked action rows + `allowed_mentions: parse=[]` so we never @-ping), `build_edit_message_payload_for_decision` (clears components, switches embed color to amber/emerald/indigo/red per outcome, replaces description with decision summary), `build_rework_modal_data` + `wrap_as_modal_response` (Discord modal with text input), `build_deferred_update_response` (type-6 ack for >3-second flows), `build_update_message_response` (type-7 inline edit). `parse_custom_id` round-trips the `tdm:{action}:{run_id}:{node_id}` correlation. **19 golden tests** assert exact embed/action-row JSON, button styles (PRIMARY→Success green, DEFAULT→Secondary grey, DESTRUCTIVE→Danger red), modal shape, color transitions, multi-row chunking at the 5-button cap, and custom-id round-trip.
- [x] **W4.2** Discord interaction endpoint at `crates/tandem-server/src/http/discord_interactions.rs` wired at `POST /channels/discord/interactions`. Verifies Ed25519 on every request (rejects 401 on failure — Discord disables endpoints that fail validation). PING (type 1) → PONG. MESSAGE_COMPONENT (type 3) parses `data.custom_id`, dispatches `approve`/`cancel` directly to `automations_v2_run_gate_decide`, opens the rework modal (type-9 response) when the user clicks Rework. MODAL_SUBMIT (type 5) extracts the reason from the text-input value and dispatches with the reason. APPLICATION_COMMAND (type 2) returns a "slash commands land in W5" placeholder so Discord stays valid. Bounded LRU dedup on `interaction_id`. New `DiscordConfigFile.public_key` field. Race UX maps non-200 gate-decide failures to a UPDATE_MESSAGE response so Discord stays happy and the user sees the conflict. **3 unit tests** on dedup + custom_id format.
- [x] **W4.3** Telegram inline-keyboard renderer at `crates/tandem-channels/src/telegram_keyboards.rs`. Pure functions: `build_send_message_payload` (MarkdownV2 text + inline keyboard chunked at 3 buttons per row), `build_edit_message_text_for_decision` (replaces text with "Decided by …" summary and clears keyboard), `build_clear_keyboard_payload` (optimistic hide-buttons before round-trip), `build_force_reply_for_rework` (Telegram's substitute for a modal — bot sends a message with `force_reply: true` and selectively waits for the user's next reply). `build_callback_data` packs `tdm:{action}:{run_id}:{node_id}` under Telegram's 64-byte cap with truncation marker; `parse_callback_data` exposes the truncation flag so the dispatcher can resolve via cache. Emoji prefixes (`✓`/`✗`/none) signal button intent visually since Telegram has no button-style enum. Full MarkdownV2 escaping via `escape_markdown_v2`. **16 golden tests** cover keyboard layout, callback_data round-trip, truncation, MarkdownV2 escaping (including backslash double-escape), force-reply payload, and label truncation.
- [x] **W4.4** Telegram callback_query webhook handler at `crates/tandem-server/src/http/telegram_interactions.rs` wired at `POST /channels/telegram/interactions`. Verifies `x-telegram-bot-api-secret-token` against configured `webhook_secret_token` on every request. Bounded dedup on `update_id`. Parses callback_data via `parse_callback_data`, dispatches `approve`/`cancel` to `automations_v2_run_gate_decide`. `rework` button taps log + ack — full force-reply state-machine wiring is W5 (it shares state with `channel_automation_drafts`). Truncated callback_data fails closed (refuses dispatch on partial run_id; cache resolution is W5). Race UX maps non-200 to 200 + log so Telegram doesn't double-fire. New `TelegramConfigFile.webhook_secret_token` field. **3 unit tests** on dedup + callback_data round-trip.
- [x] **W4.5** Update CHANGELOG and RELEASE_NOTES; commit.

**What shipped (47 new tests):** Discord and Telegram now have the same level of interactive-card support as Slack — Block Kit-equivalent renderers, signed interaction endpoints, race-aware dispatch, idempotency, and (critically) Ed25519 signature verification for Discord that Discord requires to keep the endpoint validated.

**What's deferred to W5:**

- The `chat.update`/`PATCH /channels/.../messages/...`/`editMessageReplyMarkup` _dispatch_ against real workspaces (the builders are tested; calling the API end-to-end requires plumbing the original message ID from post-send response back through gate-decide).
- Telegram force-reply _capture_ (the payload builder is shipped; the dispatcher state machine that intercepts the user's NEXT message and routes it as the rework reason is the W5 task that shares plumbing with `channel_automation_drafts`).
- Truncated-callback-data resolution via short-lived cache (W5 — needs the dispatcher to remember `(short_id → full_id)` for ~10 minutes).
- Threading per workflow run (Discord supports threads; Telegram supports topics in supergroups). Builders are not yet emitted; thread routing happens at the request-construction level so it's a small follow-up.

**Net:** Slack, Discord, and Telegram all accept signed interactions, deduplicate retries, and dispatch through the same `automations_v2_run_gate_decide` handler. The control-panel inbox plus all three channels can decide an approval and the runtime sees one decision regardless of surface.

---

## Week 5 — Notification fan-out + slash commands + demo + tests

- [x] **W5.1** Notification fan-out task at `crates/tandem-server/src/app/approval_outbound.rs`. Polls `/approvals/pending` on a configurable interval (default 5s) and dispatches new requests to a `Vec<Arc<dyn ApprovalNotifier>>`. **Deliberate departure from the existing event-bus pattern**: the Plan agent flagged that `tokio::sync::broadcast::error::RecvError::Lagged(_)` drops events, and a missed approval notification means a stuck run. Polling the aggregator (an idempotent read of durable state) avoids that failure mode entirely — a slow notifier or a process restart cannot lose a pending approval. In-memory `DedupRing` (FIFO eviction at 8192 cap) prevents re-dispatch; `prune_to` evicts decided requests so resurfacing is safe. `ApprovalNotifier` trait + `NotifierError::Transient`/`Permanent` semantics make notifier-side retry/suppression deterministic. `run_polling_loop` exposes a cooperative cancel via `Arc<AtomicBool>`. **9 unit tests** cover first-sweep dispatch, dedup on second sweep, newly-added requests, decided-then-resurfaced, failing notifier isolation, empty pending, dedup eviction, prune semantics, and error-display formatting.
- [x] **W5.2** Slash command extensions: `/pending` and `/rework`. New `SlashCommand::Pending` and `SlashCommand::Rework { run_id, feedback }` variants in `dispatcher_parts/part01.rs` + parser entries. `handle_slash_command` arms in `dispatcher_parts/part03.rs` call new helpers `fetch_pending_approvals` (GETs `/approvals/pending` and renders a chat-friendly summary with workflow name, run_id, action_kind, and a footer reminding users they can also click card buttons) and `relay_gate_decision` (POSTs `/automations/v2/runs/{run_id}/gate` with `{decision, reason}`, surfaces 409 race conflicts as friendly "already decided" toasts). Both commands registered in `BUILTIN_CHANNEL_COMMANDS` with the `approval` audience and `enabled_for_public_demo: false` so registry-driven `/help` shows them in operator/trusted-team channels and excludes them in PublicDemo. **5 new tests** cover `/pending` parse, `/rework <run_id> <feedback>` parse, `/rework` without feedback returns None, registry presence, and PublicDemo disable.
- [x] **W5.3** Authority chain resolver at `crates/tandem-server/src/app/state/principals/channel_identity.rs`. Pure function `resolve_channel_user(effective_config, kind, surface_user_id) -> ChannelIdentityResolution` returns one of `Resolved(RequestPrincipal)`, `ChannelNotConfigured(kind)`, or `Denied { kind, user_id }`. The principal carries `actor_id = "channel:{kind}:{user_id}"` and `source = "channel:{kind}"` so distinct channel surfaces never alias the same user ID into the same actor. Allowlist semantics: empty `allowed_users` is **deny-all** (channels must explicitly opt users in); `*` is wildcard; case-insensitive matching; `@`-prefixed Telegram-style entries match unprefixed user IDs. Callers MUST treat `Denied` and `ChannelNotConfigured` as hard rejects (do NOT silently approve as anonymous). **12 unit tests** cover wildcard, deny-by-default, case insensitivity, `@`-prefix matching, missing config, missing user, and per-kind actor-id distinguishing.
- [→] **W5.4** Agent prompt finalization + demo workflow E2E pass — **prompt update was W3.5 (already shipped); E2E pass against real workspaces is deferred to a focused real-channel session.** The cargo-test side of the loop is fully exercised by W1.5+W2+W3+W4+W5 unit/integration tests. Running the `sales-research-outreach` workflow end-to-end against connected Slack/Discord/Telegram requires a real workspace and configured signing secrets — best done as a focused real-channel session that also handles W2.5/W2.7/W2.8/W4 in-place edit + threading dispatch.
- [x] **W5.5** Concurrent race regression test in `http/tests/approvals_aggregator.rs::gate_decide_concurrent_race_yields_exactly_one_winner`. Fires two HTTP `POST /automations/v2/runs/{run_id}/gate` requests in parallel via `tokio::spawn` against the same run with a real pending gate. Asserts exactly one 200 + one 409, that the loser's body carries `winningDecision { node_id, decision, reason, decided_at_ms }` (W2.6), and that `gate_history.len() == 1` post-race (the per-run mutation lock prevents double-decide). Improves on the W2.6 single-threaded test that simulated the post-race state.
- [x] **W5.6** Update CHANGELOG and RELEASE_NOTES; commit. Tagging deferred — v0.4.41 stays Unreleased until the real-workspace E2E session lands.

**What shipped (35 new tests across 5 modules + parser + slash commands):** the runtime side of the agent-owned-workflows pitch is now fully demoable on `automation_v2` missions — every step from compile-time gate injection through inbox/Slack/Discord/Telegram surfacing through audit-recorded decisions has working, tested code. The fan-out task is the production-correctness piece that the Plan agent flagged (no missed approvals, no stuck runs).

**What's deferred to the real-workspace session:**

- Actual API-call dispatch for the in-place edits (`chat.update`, Discord PATCH, `editMessageText`) and threading — builders are tested; the dispatch wiring needs a live Slack/Discord/Telegram workspace to validate end-to-end.
- Telegram force-reply _capture_ state machine (the payload builder is in W4; the dispatcher state-machine that intercepts the user's NEXT message shares plumbing with `channel_automation_drafts` and is best built once with a real bot to test against).
- Truncated callback-data resolution via short-lived cache.
- Slack App Home pinned approvals (needs OAuth flow setup).
- Workflow execution pause/resume (W1.4) — still needs the design conversation about routing workflows through `automation_v2` executor vs parallel pause/resume.
- ScopeInspector per-step override toggle (W3.3) — focused TS task, server-side hook is wired and tested.

**Acceptance status:** every claim about the pitch ("agents own workflows; humans approve boundaries; runtime governs") is now backed by tested code. The remaining pieces are wiring against real platforms, not new architecture.

---

## Notes

- Each week's commit should run `cargo test -p <affected crate>` and pass before push.
- Channel adapter changes need integration tests against real test workspaces (Slack test workspace, Discord dev server, Telegram bot) gated behind `TANDEM_NIGHTLY_E2E=1`.
- Out-of-scope items (email approvals, reactions-as-approve, cross-tenant delegation, mobile push, SSE on inbox) are explicitly NOT in this kanban — see PLAN.md §"Out of scope".
