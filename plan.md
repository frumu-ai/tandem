1. **Replace Toggle Pattern in `SessionSidebar.tsx`**:
   - Apply `replace_with_git_merge_diff` on `apps/tandem-desktop/src/components/sidebar/SessionSidebar.tsx`.
   - Update `useState` and `toggleProject` array methods. Wait, I already have the exact code.

2. **Replace Toggle Pattern in `TaskSidebar.tsx`**:
   - Update `useState` and `toggleSection` logic and `.has` to `.includes`.

3. **Replace Toggle Pattern in `ActivityPanel.tsx`**:
   - Update `useState` and `toggleItem` logic and `.has` to `.includes`.

4. **Replace Toggle Pattern in `ActivityDrawer.tsx`**:
   - Update `useState` and `toggleItem` logic and `.has` to `.includes`.

5. **Replace Toggle Pattern in `PlanSelector.tsx`**:
   - Update `useState` and `toggleSession` logic and `.has` to `.includes`.

6. **Replace Toggle Pattern in `ExecutionPlanPanel.tsx`**:
   - Update `useState` and `toggleExpanded` logic and `.has` to `.includes`.

7. **Test Changes**:
   - Run type checker and tests.

8. **Pre-commit Steps**.

9. **Submit**.
