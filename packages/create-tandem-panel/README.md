# create-tandem-panel

Scaffold a fully editable Tandem control panel app.

## Usage

```bash
npm create tandem-panel@latest my-panel
```

Or:

```bash
npx create-tandem-panel my-panel
```

This copies a starter app into your folder so you can customize the UI, routes, styles, and local runtime without editing `node_modules`.
